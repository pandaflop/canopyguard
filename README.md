Instructions:
Run the following commands in windows power shell, or other dependent on preference
git clone https://github.com/YOUR-USERNAME/canopyguard.git
cd canopyguard
npm install
npm run build
npm run start
